---
name: diagnose-xiaohongshu-account
description: 当需要依据小红书创作者导出的账号数据、笔记明细、主页截图和用户目标，对小红书账号进行分析、诊断、复盘或寻找下一步行动时使用。必须依次完成资料读取确认、账号阶段与经营诊断、人工校准与第一优先行动收敛三轮流程。不适用于抖音、视频号或其他平台。
---

# 小红书账号诊断

根据用户提供的文件和背景信息诊断一个小红书账号。所有事实均需可追溯，明确区分数据证据与分析推断，最终收敛出一个可验证的第一优先行动。

## 必须读取的参考文件

输出相应轮次前，必须完整读取以下文件：

- 第一轮前读取 `references/xiaohongshu-inputs.md`。
- 第二轮前读取 `references/diagnostic-protocol.md`。
- 格式化任何一轮输出前读取 `references/output-templates.md`。

所有相对路径均以本 Skill 目录为基准解析。

## 适用范围

本 Skill 仅支持小红书账号诊断，不支持抖音、视频号或其他平台。

1. 确认账号与资料属于小红书。
2. 平台不明确时，先提出一个聚焦问题进行确认。
3. 如果账号属于抖音、视频号或其他平台，停止正式诊断，并说明当前只支持小红书。
4. 不得将小红书的字段含义、判断阈值或内容单位套用到其他平台。

## 选择交互模式

除非用户明确要求不等待、跳过中间确认或一次性给出完整报告，否则使用**默认模式**。

### 默认模式

- 每次只完成一轮。
- 以该轮规定的确认问题结尾。
- 等待用户纠正或确认后再进入下一轮。

### 快速模式

- 连续完成全部三轮。
- 保留每一轮的输出内容和推理边界。
- 不得省略文件清单、账号阶段分流、证据定位、证据强度、不确定性或第一优先行动卡。
- 在最终行动卡前增加以下说明：

> 本次采用快速模式，中间结果未经用户逐轮确认。涉及账号定位、数据口径和现实约束的判断，需要用户后续复核。

跳过等待不等于跳过轮次。

## 三轮诊断流程

### 第一轮：读取与确认

读取用户提供的目录或指定文件，此轮不得开始诊断。

必须完成以下事项：

1. 确认平台为小红书。
2. 列出成功读取和读取失败的所有文件。
3. 在资料允许时，说明每个文件包含的工作表或页面、字段、记录数量和时间范围。
4. 识别时间范围不一致、指标含义不清、资料缺失和内容无法读取等问题。
5. 复述账号目标、目标用户、当前困惑和执行约束。
6. 说明缺失信息会影响哪些后续判断。
7. 只提出继续诊断所必需的最少问题。

使用第一轮输出模板。默认模式下，到此停止并请用户确认或纠正读取结果。

### 第二轮：分流与诊断

先判断账号所处阶段，再诊断对应问题，不得颠倒顺序。

1. 统计有效笔记数量并计算数据充分度得分。
2. 按目标用户、用户问题和内容主题，对近期内容进行分类。
3. 默认模式下，先请用户确认内容分类，再将内容一致性得分视为最终结果。
4. 将账号分流至以下一种诊断：
   - 完整经营诊断；
   - 冷启动诊断；
   - 定位诊断。
5. 按规定结构输出具有优先级的候选诊断。

每项重要诊断必须区分：

- 数据事实；
- 证据定位；
- 诊断解释；
- 可验证假设；
- A、B 或 C 级证据强度；
- 不确定性；
- 候选行动与验证指标。

不得把相关性表述为已经证实的因果关系，不得虚构小红书行业基准、官方规则或缺失字段，优先使用账号自身数据进行内部对比。

默认模式下，到此停止并询问用户：哪些事实、分类或解释需要修正。

### 第三轮：人工校准与行动收敛

与用户完成“对—问—选—改”：

1. **对数据**：核对一至两条决定优先级的关键数据及其证据位置。
2. **问理由**：解释事实如何支持判断，同时列出其他合理解释。
3. **选一个**：比较证据、影响、可控性、成本和验证速度，只保留一个方向。
4. **改行动**：结合用户的时间、能力、资源和意愿，缩小为最小行动。

最后只输出一张第一优先行动卡，包含行动、证据、假设、执行边界、观察指标、复盘日期和继续条件。

## 账号阶段分流

详细规则见 `references/diagnostic-protocol.md`，按以下顺序判断：

```text
有效笔记不足5篇 → 冷启动诊断
有效笔记达到5篇 → 先确认内容分类
内容一致性0–2分 → 定位诊断
内容一致性3–6分 → 判断数据充分度
数据充分度5–6分 → 完整经营诊断
数据充分度0–3分 → 冷启动诊断
数据充分度4分 → 执行边界判断
```

所有阈值均为课程 V1 试行标准，不是小红书官方规则。

## 证据规则

- 每项重要事实都必须定位到本地文件。
- 优先使用格式：`文件名｜工作表/页面｜行号或记录标识｜字段｜时间范围`。
- 无法提供准确行号时，至少给出文件、字段、日期、笔记标题或其他可查询的记录标识。
- 对计算结果说明公式和输入数据来源。
- 无法追溯的事实不得标记为强证据。
- 证据强度描述的是证据对诊断判断的支持程度，不代表模型可信度，也不代表行动一定成功。
- 必须用具体文字说明不确定性。

## 异常处理

遇到文件无法读取、统计周期冲突、目标缺失、样本过少、内容方向不清、指标口径不明，或数据与用户描述矛盾时，不得输出强结论。

应当依次：

1. 指明问题；
2. 降低证据强度；
3. 说明当前无法回答什么；
4. 请求最少的缺失信息，或转入冷启动诊断、定位诊断。

不得静默补全缺失数据。
